---
title: Numeral Converter
description: Convert between every numeral type convnum supports. Type in any field to see all conversions.
tableOfContents: false
---

import NumeralConverter from '../../../components/NumeralConverter.tsx'

Type a value into **any** field to see it converted across every supported
numeral type in real time. Each field has its own formatting controls (case,
prefix, script, zero-padding, UK/US style) that update the output immediately.

<NumeralConverter client:load />
